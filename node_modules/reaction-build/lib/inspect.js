'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = inspect;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

var _crossEnvironment = require('cross-environment');

var _reactionBase = require('reaction-base');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function inspect() {
  for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  _reactionBase.log.info.apply(_reactionBase.log, args);
  if ((0, _crossEnvironment.getRuntime)() === 'browser') {
    // eslint-disable-next-line no-undef
    var browserWindow = window;
    if (browserWindow.reaction) {
      var item = args;
      if (args.length) {
        if (args.length === 1) {
          item = args[0];
        } else if (args.length === 2) {
          item = args[1];
        } else if (_lodash2.default.isString(args[0])) {
          item.shift();
        }
      }
      if (!browserWindow.reaction.inspect) browserWindow.reaction.inspect = {};
      browserWindow.reaction.inspect[args.length > 1 && _lodash2.default.isString(args[0]) ? args[0] : _lodash2.default.get(browserWindow, 'reaction.inspect.length', 1) - 1] = item;
    }
  }
}